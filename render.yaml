services:
  - type: web
    name: anloga-cashbook
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: "/((?!assets|favicon.ico|logo.png|vite.svg|_redirects).*)"
        destination: /index.html